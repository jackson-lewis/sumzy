import { TransactionDirection } from '@/types'

export default function FrequencySelector({
  direction
}: {
  direction: TransactionDirection
}) {
  return <p>Component deprecated. {direction}</p>
}
